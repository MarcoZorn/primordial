__pycache__/
*.pyc
.venv/
# checkpoints are large binaries; the telemetry is the scientific record and is kept
runs/**/state.pkl.gz
runs/**/state.pkl.gz.prev
runs/**/*.tmp
runs/*.log
runs/**/daemon.pid
runs/**/viewer.pid
runs/**/viewer.speed
runs/watchdog.pid
